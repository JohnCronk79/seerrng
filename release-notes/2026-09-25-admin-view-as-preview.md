## Admin View As preview

- Added an admin-only **View As...** selector above Settings in the avatar menu.
- The signed-in user appears first and remains the default; other users are listed alphabetically with a check mark on the selected view.
- Selecting another user previews permission-gated navigation and controls without changing the authenticated user, request ownership, or server authorization. The selection resets on a fresh login.
