SeerrNG requires a dedicated domain or subdomain and must be installed at `/`.
It does not support a URL prefix such as `/seerrng`.

This package supports `amd64` and `arm64` only. YunoHost's LDAP and portal
single sign-on are not integrated; SeerrNG manages access through its own
accounts and connected media server.
